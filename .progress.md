# TripFlowy Sprint Progress

Tracks sprint-level state. Full sprint prompts live in `/docs/tripflowy-master-plan.md`.
Update this file at the end of each session (see Part D.2 of the master plan).

**Last updated**: 2026-04-23
**Baseline GEO/AEO score**: 88/100 (pre-sprint diagnostic)

---

## ✅ Sprint 0 — Project Infrastructure (2026-04-23, completed)

- [x] Root `AGENTS.md` rewritten (<200 lines, project overview + stack + core rules)
- [x] `CLAUDE.md` replaced with symlink → `AGENTS.md`
- [x] `/app/AGENTS.md` created (App Router, JSON-LD dual-type, hreflang, Person.alternateName array rule)
- [x] `/content/AGENTS.md` created (frontmatter standard, TRUST_PHRASES rule, extraction chunk rule, forbiddenPhrases grep)
- [x] `/docs/AGENTS.md` created (internal vs public docs split)
- [x] `/scripts/AGENTS.md` created (sharp .withMetadata(), Naver pipeline, env keys)
- [x] `/content/brand.ts` created (BRAND, FOUNDER, META_DESCRIPTIONS, HERO, COMPARISON_HEADINGS, TRUST_PHRASES, AFFILIATE_DISCLOSURE)
- [x] `tsc --noEmit` clean

**Notes**: `FOUNDER.profiles` keeps `[TODO]` markers for naverBlog / linkedin / crunchbase / wikidata — to be replaced in Sprint 6. Any JSON-LD `sameAs` emitter must filter out `[TODO]` entries before rendering.

---

## 🚨 Sprint A — Immediate Fixes (Week 1, ~12h)

- [x] **A.1** 🔴 Fix `/llms.txt` 500 error + create `/llms-full.txt` (2026-04-23) — root cause: route did not exist, so `/llms.txt` matched the `[locale]` dynamic segment and hit `next-intl` with an invalid locale. Fix: added static Route Handler `app/llms.txt/route.ts` + `app/llms-full.txt/route.ts` (both `force-static`, `text/plain`), robots.txt LLM-friendly index comments. Build produces both as `○ (Static)` routes; curl with `ClaudeBot` UA → 200 OK, body contains `TripFlowy` + `Check Kim`. `[TODO]` profiles correctly filtered out of llms-full.txt.
- [x] **A.2** 🔴 Unblock `/courses` in `robots.txt` + remove from nav (2026-04-23, hybrid option C) — stripped `Disallow: /courses*` from all 10 UA blocks in `public/robots.txt` (wholesale crawler block removed; page-level `noindex` meta now the sole index-control mechanism). Header/Footer nav already had 0 `/courses` links — no component edits needed. **Kept** legacy `/courses/[id]` `noindex/nofollow` meta + sitemap exclusion (existing `data/day-courses.ts` 15 entries are placeholder content generated by Claude in a prior session, not 책킴-verified; don't promote to indexable). Added new `/courses/[city]/[slug]` route rules to Sprint 7 section + `/app/AGENTS.md` for when 책킴-verified day trips ship. Sprint 7 TODO: decide fate of legacy `/courses/[id]` catalog (delete / rename / selective migrate).
- [x] **A.3** 🔴 Korean WebSite schema description → `META_DESCRIPTIONS.homeKo` (2026-04-23) — swapped in `lib/jsonld.ts` `generateWebSiteJsonLd()` ko branch. Hardcoded "자동으로 여행 일정을 만들어주는" AI-positioning string removed; now imports `META_DESCRIPTIONS` from `@/content/brand`. Local curl `/ko` confirms JSON-LD `description` = "직접 다녀온 사람이 검증한 여행 일정 템플릿…". English branch still hardcoded — A.4 will replace.
- [x] **A.4** 🟠 English WebSite schema description → `META_DESCRIPTIONS.homeEn` (2026-04-23) — `lib/jsonld.ts` `generateWebSiteJsonLd()` else-branch now imports `META_DESCRIPTIONS.homeEn`. Ternary collapsed to one line. Local: `/en` JSON-LD `description` matches `META_DESCRIPTIONS.homeEn` exactly; old "Auto-generate day-by-day…" string grep = 0; `/ko` unchanged (no regression).
- [x] **A.5** 🟠 Hero copy + comparison heading + HowTo JSON-LD blogger-respecting rewrite (2026-04-23, expanded scope) — `messages/{ko,en}.json` hero.heading → `HERO.h1Ko/En`, new hero.subTagline slot → `HERO.subTaglineKo/En` (rendered in `app/[locale]/page.tsx` beneath existing subheading), whyDifferent.heading → `COMPARISON_HEADINGS.ko/en`, howItWorks step titles/descs rewritten to blogger-respectful / 책킴-authority wording, whyDifferent.aiTitle rewritten from forbidden "AI Travel Planners"/"AI 여행 플래너" to sanctioned contrast "AI-Generated Plans"/"AI 생성 일정". Added `META_DESCRIPTIONS.howToKo/howToEn` to `/content/brand.ts`; `generateHowToJsonLd()` now imports them. Homepage `generateMetadata` also swapped to `META_DESCRIPTIONS.homeKo/homeEn` (hardcoded `<meta description>` still had "자동으로…블로그 검색 그만" / "auto-generate" which A.3/A.4 didn't touch). Final local sweep: 11 forbidden phrases all 0 hits; 11 new phrases all rendering. Expanded deliverables beyond original 0.5h spec: also caught & fixed two off-spec hardcoded strings (metadata description + aiTitle) because master plan Part A principle #1 forbids those phrases site-wide.
- [x] **A.6** 🟡 Person schema `alternateName` → array across all pages (2026-04-23) — `lib/jsonld.ts` `generateArticleJsonLd()` author block: replaced `alternateName: author.nickname[locale]` (single string, locale-switched) with `alternateName: FOUNDER.alternateName` (5-entry array: Check Kim, 책킴, travelkkkim, Huiwon Kim, Chaekkim). Since this helper is the sole Person-schema emitter across the site (all posts, no inlined Person schemas elsewhere, no `/content/guides/` folder), one file change propagates to all 18 post pages. `Author.nickname` type kept for UI byline use (A.8 owns display-name format). Organization/WebSite `alternateName: "트플"` (single string) kept as-is — that's BRAND-level, schema.org allows single or array, out of A.6 scope. `/content/AGENTS.md` expanded with 3 new rule sections (Person-alternateName array rule + retired Patrick Kim warning, comparison column header rewrite rule retroactively documenting A.5, metadata-description 4-layer audit scope retroactively documenting A.5 gap). Local verification on `/posts/tokyo-disneyland-guide` and `/ko/posts/tokyo-disneyland-guide`: Person.alternateName is JSON array with all 5 entries; single-string alternateName on post pages = 0; Patrick Kim = 0.
- [x] **A.7** 🟡 Bio update + jobTitle + FOUNDER adapter + sameAs wiring (2026-04-23, expanded scope) — `lib/authors.ts` refactored to adapter (`{ id, brand: FOUNDER }` + `image?`/`url?`), eliminating duplicated name/bio/role/expertise fields. New `getAuthorIdentity(author, locale)` helper centralizes the locale ternary and filters `[TODO]` profile URLs for Person.sameAs. `types/index.ts` `Author` type simplified (imports `typeof FOUNDER`). `lib/jsonld.ts` `generateArticleJsonLd` author block rewired through identity helper — now emits `name` (primary: 책킴/Check Kim), `alternateName` (6-entry array), `jobTitle` (TripFlowy Founder · 여행 크리에이터 / Founder, TripFlowy · Travel Creator), `description` (FOUNDER.bioKo/bioEn — "아시아 15개국 50+ 도시" / "50+ cities in 15+ Asian countries"), `knowsAbout` (7 entries incl Asia travel + day trips), and `sameAs` (3 URLs: instagram + naverBlog + youtube, filtered from FOUNDER.profiles with [TODO] stripped). `app/[locale]/posts/[slug]/page.tsx` consumers updated (5 points): byline + author-box name/role/bio now resolve through identity. Redundant `author.nickname` paren render removed (A.8 will re-add cross-locale "책킴 (Check Kim)" pair properly). Local verification: ko + en post JSON-LD exact match; "tripflowy 창업자" 0 hits (소문자 t 박멸); Patrick Kim 0; [TODO] 0 leakage.
- [x] **A.8** 🟡 Bilingual author box "책킴 (Check Kim)" / "Huiwon Kim (Check Kim)" (2026-04-23) — added `displayName` field to `getAuthorIdentity()` in `lib/authors.ts` (ko → `${brand.nameKo} (${brand.nameEn})` = "책킴 (Check Kim)"; en → `${brand.legalName} (${brand.nameEn})` = "Huiwon Kim (Check Kim)"). Swapped author-box headline in `app/[locale]/posts/[slug]/page.tsx` from `{identity.name}` to `{identity.displayName}`. Hero bylines (line 254, 276) intentionally kept at single-name `{identity.name}` — short metadata strip, concision over completeness. JSON-LD Person.name unchanged (stays locale-primary per A.7). Local verification: ko byline = "책킴" alone (1 `>책킴<` span), ko author-box = "책킴 (Check Kim)" (2 hits incl og variant); en author-box = "Huiwon Kim (Check Kim)"; `김희원` legal-name UI leakage = **0** (confined to JSON-LD alternateName + brand.legalName constant); Patrick Kim = 0.
- [x] **A.9** 🟡 Sitemap audit — add x-default hreflang, exclude /planner, document exclusions (2026-04-23) — `app/sitemap.ts`: added `x-default` alternate (points at EN URL) to both static pages and post entries via shared `hreflang()` helper. Removed `/planner` from `staticPages` (form page + unbounded `?destinations=...` query combinations; low SEO value, discovered via internal links). Kept existing exclusions for `/courses` (A.2 decision) and `/api`. Verified locally: 13 `<url>` entries (4 static: `/`, `/posts`, `/tours`, `/hotels` + 9 post details), hreflang `en`/`ko`/`x-default` = 13 each (every URL has all 3 + self-reference), `/planner` / `/courses` / `/api` = 0 hits, per-post `<lastmod>` = `updatedAt ?? publishedAt`, static `<lastmod>` = SITE_FRESHNESS (newest post date, not `new Date()`).
- [x] **A.10** 🟡 English + Korean affiliate disclosure → `AFFILIATE_DISCLOSURE` import (2026-04-23, text-align hotfix 2026-04-24: `text-right` → `text-left` on the disclosure `<p>`; right-alignment was breaking natural reading flow for both locales — EN clause "…we may earn a small / commission if you book…" and KO clause "…직접 방문 / 하고 검증한 곳만…" were wrapping mid-phrase. `max-w-xl` width cap kept. 1-line change.) — `components/layout/Footer.tsx`: swapped `{t("affiliateDisclosure")}` to direct `AFFILIATE_DISCLOSURE[locale]` import from `@/content/brand`. Removed now-dead `footer.affiliateDisclosure` keys from `messages/ko.json` + `messages/en.json` (single consumer, verified by grep). Result: EN footer now renders full FTC-style disclosure ("Affiliate Disclosure: Some links on this site are affiliate links — we may earn a small commission if you book through them, at no additional cost to you. We only recommend tours and accommodations we've personally tested.") replacing the prior truncated i18n version that lacked the integrity clause. KO footer gains the "직접 방문하고 검증한 곳만 추천합니다" trust phrase suffix. Width cap `max-w-xl` added to the disclosure `<p>` so the now-longer text wraps cleanly in the footer bottom row.
- [ ] **A.11** 🟡 Home hero trust stat box (FOUNDER.stats) (2h)
- [ ] **A.12** 🟡 Planner result per-spot `lastVerified` display (3h — simple now, full schema in Sprint 3)

---

## 📊 Sprint 1 — Analytics Infrastructure (Week 2, 1d)

**Gate**: Must be completed before any content sprint (principle #4 — no measurement = no ROI).

- [ ] Vercel `/middleware.ts` — referer + UA logging, AI crawler + AI assistant detection, UTM parsing
- [ ] Vercel KV log schema wired
- [ ] `/app/admin/analytics/page.tsx` dashboard (AI crawlers / AI referrals / landing top 10 / UTM campaigns)
- [ ] Signup form "어디서 저희를 알게 되셨나요?" field (or log as N/A if no form)
- [ ] GA4 custom channel group "AI Assistants"
- [ ] `/docs/utm-convention.md` + `/docs/analytics-regex.md`
- [ ] `curl -A "GPTBot"` smoke test logged

---

## ⏭️ Sprint 2 — SKIPPED (already implemented)

Existing site already has: Organization / WebSite / HowTo / Article+TravelGuide / FAQPage / BreadcrumbList / speakable JSON-LD. No separate sprint needed.

---

## 🖼️ Sprint 3 — Image Pipeline + Spot Schema (Week 2, 2–3d)

Blocks Sprint 4 & Sprint 7. Must complete before city content expansion.

- [ ] `/content/schemas/spot.ts` — `Spot` type per master plan
- [ ] `/content/spots/tokyo/` sample stubs (5 spots, placeholders OK)
- [ ] `/docs/lightroom-workflow.md` — Sony A7M5 EXIF workflow + "Remove Location Info unchecked" warning
- [ ] `/scripts/process-image.ts` — sharp + `.withMetadata()` (EXIF preserved), AVIF/WebP out, originals backup to `/content/images-original/`
- [ ] `/scripts/naver-to-tripflowy.ts` — Naver HTML → guide markdown with extraction chunk + trust phrase injection
- [ ] `/scripts/extract-spots.ts` — Claude API spot extraction
- [ ] `/scripts/check-stale-spots.ts` — warns on `lastVerified > 12 months` (wire into CI)

---

## 🔗 Sprint 4 — Hub Pages (Week 3, 2d)

English-first, Korean adaptation (not translation).

- [ ] `/vs/` comparison pages × 5 (wanderlog, mindtrip, layla, tripit, google-travel)
- [ ] `/learn/` glossary pages × 10 (what-is-a-curated-travel-platform, …, osaka-vs-kyoto-for-first-timers)
- [ ] `/for/` persona pages × 5 (solo, family-with-kids, with-parents, group-of-friends, budget)
- [ ] `/alternatives/` × 3–5 (wanderlog, tripit, mindtrip)
- [ ] All pages: FAQPage JSON-LD + extraction chunk + TRUST_PHRASES + bidirectional hreflang + `FOUNDER` author

---

## 🔗 Sprint 5 — Naver Homepage Link + Legacy Posts (Week 6+)

Book-end of the content push; needs Sprint 1 analytics and tidied Sprint A hero in place.

**Pre-decisions (책킴)**: active/dormant on Naver? legacy post count by city? high-traffic posts (100+/mo)?

- [ ] **Track A — retroactive** `/scripts/naver-retroactive-link.ts` (3 CTA variants per post, UTM-tagged)
- [ ] **Track A** prioritized rollout: traffic ≥100/mo → Tokyo/Osaka → rest (manual add by 책킴)
- [ ] **Track B — prospective** `/scripts/naver-link-generator.ts` interactive CLI
- [ ] `/app/page.tsx` tweaks: if `utm_source=naver_blog` prioritize Korean content; campaign city drives CTA
- [ ] Analytics dashboard extension: Naver referral traffic, utm_content top 10, per-post conversion
- [ ] `/docs/canonical-rules.md` (TripFlowy self-canonical; link back to Naver original in body)
- [ ] `/docs/naver-writing-guide.md` (year in title, mid-body link placement, "책킴이 만든" phrasing)

---

## 🏛️ Sprint 6 — External Authority + Wikidata (Week 2, 0.5d code + ~3h manual)

**책킴 manual tasks (priority tiers):**

- **Tier 1 (today, ~30m)**: LinkedIn headline · Instagram bio · YouTube channel desc · Naver blog profile · GSC + Bing Webmaster Tools
- **Tier 2 (this week, ~1.5h)**: Wikidata Person + Organization items (do with Claude) · Crunchbase Person + Org · Naver Search Advisor · AlternativeTo.net
- **Tier 3 (this month, ~1.5h)**: G2 / Capterra via Gartner Digital Markets · Product Hunt "Upcoming"

**Claude Code follow-up:**

- [ ] Replace `FOUNDER.profiles` `[TODO]` URLs with real LinkedIn / Crunchbase / Wikidata IDs
- [ ] Add `wikidataOrg` field for TripFlowy organization item
- [ ] Audit all Organization / Person `sameAs` arrays include the new URLs (filter `[TODO]`)
- [ ] Footer "Find us on: LinkedIn · Crunchbase · AlternativeTo · G2 · Wikidata"
- [ ] GSC / Bing IndexNow re-index requests for home + /ko + all /courses/tokyo/* + top guides

---

## 🌆 Sprint 7 — City Content + Day Trips (Week 3–8)

**Gate**: Sprint 3 (image pipeline) must be done.

- [ ] **Week 3–4 Tokyo**: 6 day-trip pages (`/courses/tokyo/{kawagoe,chichibu,mt-fuji,nikko,kamakura,hakone}`) + existing guide updates
- [ ] **Week 4–5 Osaka**: main `/city/osaka` + 5 guides + 3 neighborhoods + 3 day trips (Nara, Himeji, Koyasan)
- [ ] **Week 5–6 Kyoto**: main content
- [ ] **Week 6 Fukuoka**: main content
- [ ] **Week 7 Bangkok**: main content (Naver-source adaptation)
- [ ] **Week 8 Da Nang**: main content (Naver-source adaptation)

Per-page requirements: Article + TravelGuide dual schema · FAQPage w/ speakable · `FOUNDER` author box with alternateName array · `visitedAt` + `lastVerified` · bidirectional hreflang · `rel="sponsored"` on affiliate links · `AFFILIATE_DISCLOSURE` in body · images processed through `process-image.ts`.

**New `/courses/[city]/[slug]` route requirements (set in Sprint A.2):**
- Route shape: `/courses/[city]/[slug]` (e.g. `/courses/tokyo/kawagoe`) — distinct from legacy `/courses/[id]`.
- `robots`: `{ index: true, follow: true }` (opposite of legacy catalog).
- `alternates.canonical`: self-referential **absolute** URL (`https://www.tripflowy.com/courses/tokyo/kawagoe` or the `/ko/` equivalent).
- **Must** be emitted by `app/sitemap.ts` — update the sitemap module when adding these routes.
- hreflang: en ↔ ko + `x-default` pointing at the EN URL.
- JSON-LD: Article + TravelGuide dual type, FAQPage (speakable if applicable), BreadcrumbList, author = `FOUNDER` with `alternateName` array.
- Frontmatter / page fields: `visitedAt` + `lastVerified` (required), author `FOUNDER.nameKo/nameEn`.
- Content gate: only cover places 책킴 personally visited — no AI-generated placeholders.

**Legacy `/courses/[id]` handling (decided in Sprint A.2):**
- Keep `noindex/nofollow` meta, keep sitemap exclusion. Do not promote to the indexable shape.
- When Sprint 7 lands, decide: delete, rename (e.g. `/day-plans/[id]`), or selectively migrate individual entries after 책킴 verification.

---

## 🚀 V1.1 — Collaboration Features (3 months out)

Entry criteria (need ≥ 3 of 5):

- [ ] GEO: TripFlowy mentioned in ≥30% of 10 tracked LLM queries
- [ ] MAU: ≥1,000 unique planner users / month
- [ ] Affiliate revenue: ≥$500 / month sustained
- [ ] External authority: Wikidata + G2 + Crunchbase + AlternativeTo all live + ≥5 listicle inclusions
- [ ] City coverage: Tier-S 6 cities (Tokyo / Osaka / Kyoto / Fukuoka / Bangkok / Da Nang) main content done

Pre-V1.1 decisions (책킴): Tier-1 partner (KAL / Klook / MyRealTrip)? beta-user pool (제로투원 / Naver readers)? pricing (free / pro tier)?

Sketched sprints: Sprint 8 (group invite + swipe vote + Kakao share) · Sprint 9 (realtime co-edit) · Sprint 10 (Naver Maps saved places integration).

---

## Session Log

### 2026-04-23/24 — Sprint A.8 + A.9 + A.10 session (deployed)

**Completed (3 sprints, 4 commits):**
- **A.8** — Bilingual author-box display name. Added `displayName` to `getAuthorIdentity` (ko → `"책킴 (Check Kim)"` via `nameKo + nameEn`, en → `"Huiwon Kim (Check Kim)"` via `legalName + nameEn`). Hero byline stays single-name `{identity.name}`; only bottom author-box uses the paired form. JSON-LD `Person.name` unchanged (locale-primary, A.7). `김희원` legal-name UI leakage: 0. Commit `92e7eae`. **Part F [엔티티 일관성] 섹션 5/5 완료.**
- **A.9** — Sitemap `x-default` hreflang + `/planner` exclusion. `app/sitemap.ts` single file edit: extracted `hreflang(path)` helper emitting `{ en, ko, "x-default" }` triple (x-default → EN URL). Removed `/planner` (form page + unbounded `?destinations=...` query space). Kept exclusions for `/courses` (A.2) and `/api`. Verified: 13 URLs (4 static + 9 posts), hreflang `en/ko/x-default` = 13 each, zero hits for planner/courses/api in `<loc>`. Per-post `<lastmod>` from `updatedAt ?? publishedAt`; static uses `SITE_FRESHNESS` (newest post date, not `new Date()`). Commit `e905a40`.
- **A.10** — Footer affiliate disclosure → `AFFILIATE_DISCLOSURE` import. Swapped `Footer.tsx` from `t("affiliateDisclosure")` to direct `AFFILIATE_DISCLOSURE[locale]`; deleted dead `footer.affiliateDisclosure` keys from `messages/{ko,en}.json` (grep confirmed single consumer). EN footer gains FTC integrity clause ("We only recommend tours and accommodations we've personally tested."), KO footer gains trust-phrase clause ("직접 방문하고 검증한 곳만 추천합니다"). Commit `2240380`.
- **A.10 text-align hotfix** (2026-04-24) — `text-right` → `text-left` on disclosure `<p>`. Right-alignment was wrapping clauses mid-phrase (EN: "...we may earn a small / commission..." / KO: "...직접 방문 / 하고 검증한 곳만..."). `max-w-xl` width cap preserved. Commit `abe683d`.

**Part F scorecard after this session:**
- [브랜드 일관성] — all brand-shaped footer + sitemap strings now import from `@/content/brand`; zero hardcoding; forbidden-phrase checks unchanged from A.5 (still 0).
- [엔티티 일관성] — **5/5 closed** after A.8. Person.alternateName 6-entry array, jobTitle "TripFlowy Founder" unified, sameAs 3 URLs (instagram + naverBlog + youtube), zero Patrick Kim, author-box bilingual pair rendering.
- [기술 GEO] — hreflang 3-way (en/ko/x-default) on every sitemap URL, lastmod accurate per post, sitemap excludes per A.2/A.9 decisions.
- [수익화] — FTC-compliant disclosure on both locales. `rel="sponsored"` on affiliate anchors + UTM still pending (Sprint 7).
- [문서] — `.progress.md` comprehensive; `/app/AGENTS.md` routing rules + `/content/AGENTS.md` Person schema + comparison-copy + metadata-audit rules all current.

**Sprint A progress**: **10/12 closed** (A.1 → A.10). Remaining: **A.11** (home hero trust-stat box, 2h) and **A.12** (planner per-spot lastVerified + open-gap phrase cleanup, 3h).

---

## Next Session Briefing — Sprint A.11 (home hero trust-stat box)

**Goal**: Surface `FOUNDER.stats` on the homepage hero so LLMs chunk authority signals in the first viewport. Spec: "이 사이트의 모든 스팟은 책킴(Check Kim)이 직접 가본 곳입니다" caption + 3 stat chips + optional 50×50 profile photo.

### Key file locations

- **Hero render**: `app/[locale]/page.tsx` lines 89–144 (section `.relative.isolate.overflow-hidden.bg-slate-950`). Current composition: background photo `/images/city-heroes/tokyo.jpg` (line 91), dark overlay, left column (badge → h1 → headingHighlight → subheading → subTagline → CheckCircle bullet row at line 125–132), right column (`<QuickPlanner/>` card).
- **Brand constants to use**:
  - `FOUNDER.stats` at `content/brand.ts:90` — 3 entries: `[{ ko: "일본 20+ 방문 · 아시아 15개국 50+ 도시", en: "20+ trips to Japan · 15+ countries, 50+ cities in Asia" }, { ko: "2025년 비행기 64회", en: "64 flights in 2025" }, { ko: "검증 스팟 200+", en: "200+ verified spots" }]`. Already-imported pattern: `import { FOUNDER } from "@/content/brand"` (see `lib/jsonld.ts`, `lib/authors.ts`).
  - `FOUNDER.nameKo` / `FOUNDER.nameEn` for the caption (책킴 / Check Kim).
  - `HERO.badgeKo` / `HERO.badgeEn` — already wired via i18n `hero.badge`; don't duplicate.
- **i18n keys**: hero copy currently flows through `messages/{ko,en}.json` `hero.*`. For the new caption ("이 사이트의 모든 스팟은 책킴이 직접 가본 곳입니다" / "Every spot on this site is a place Check Kim has personally visited") — either add new `hero.trustCaption` keys OR hardcode with `FOUNDER.nameKo/nameEn` interpolation. Prefer i18n so future copy tweaks land in one locale-aware place (A.5 pattern).
- **Existing CheckCircle bullet row** (`hero.destinations`/`hero.dayByDay`/`hero.affiliate` at page.tsx:125–132) is the current "proof points" strip. Decision call at A.11 start: replace that row with the stats strip, OR keep both (stats above, bullets below).

### A.5-established copy tone (must keep)

- **Korean body** uses `트플` shorthand (not 트립플로위). English stays TripFlowy. Don't plant brand name into first-person-style caption.
- Caption voice: knowledgeable-friend, not marketer. Avoid "perfect", "amazing", generic hype.
- Preserve the field-tested positioning — any new string must pass `BRAND.forbiddenPhrases` grep: no "AI planner", "auto-generate", "블로그 그만 보세요", "AI 여행 플래너", etc.
- No "Patrick Kim", no legal name "김희원" in visible UI (Part F entity rule).

### Responsive + visual design notes

- Hero uses `grid lg:grid-cols-[1.1fr_1fr]`. Stats should live in the left column with the copy — don't cram into the QuickPlanner card.
- Current mobile behavior: `hidden sm:flex` on the CheckCircle row hides it on `< sm`. Decide whether stats follow same rule (hide on mobile) or need a condensed variant. Recommendation: show stats on mobile (authority signal is valuable on small viewports too), but compact to vertical stack.
- Photo placeholder: `FOUNDER` has no canonical `photo` field yet (A.7 noted this). For A.11, either:
  - (a) Skip the photo slot entirely — stats speak for themselves. Lowest friction.
  - (b) Add a `/public/images/authors/check-kim.jpg` placeholder (50×50, neutral background) and wire via `next/image`. Requires 책킴 to provide the actual file later — flag as a follow-up.
  - (c) Render an `<Image>` only if `author.image` is set; gracefully degrade to initials avatar otherwise. Reuses existing pattern from `app/[locale]/posts/[slug]/page.tsx:330–337` (post author-box has the exact initials-fallback).
  - Lean toward **(c)** — reusable, no block on asset delivery.
- Contrast: hero has `text-slate-950` background with `text-slate-200/300` body copy. Stats chips should use high-contrast pills (e.g. `bg-white/10 backdrop-blur border border-white/15 text-white text-xs font-medium px-3 py-1.5 rounded-full`) matching the existing badge style at page.tsx:111–114. Keep visual family consistent.

### JSON-LD implications

- `Person.description` (the FOUNDER.bio on post pages) already carries "일본 20+ · 아시아 15개국 50+ 도시" language. Don't duplicate into a new schema. A.11 is UI-only; no JSON-LD changes required.
- If adding a new `Person` emission on the homepage (Organization.founder), that's enhancement scope beyond A.11 — flag for a later sprint.

### A.11 acceptance criteria (draft)

1. 3 stat chips render on homepage hero, both locales, with `FOUNDER.stats.{ko|en}` values.
2. Trust caption ("책킴이 직접 가본 곳" pattern) visible.
3. Desktop + mobile layouts both readable without horizontal scroll.
4. No new forbidden phrases; trust phrases sanctioned by `TRUST_PHRASES` preferred.
5. `tsc --noEmit` clean, `next build` 258 pages (unchanged count).
6. Regression grep: Patrick Kim 0, 김희원 UI 0, hardcoded hero strings 0 (all via `FOUNDER.*` or i18n).

### Open-gap queue (current snapshot)

Tracked carry-overs — none block A.11:

- **A.12-scoped forbidden phrases**:
  - `app/[locale]/planner/layout.tsx:18,26,29` — "Auto-Generate Itineraries" / "자동 일정 생성" / "auto-generate verified" in planner metadata (title + og + description).
  - `lib/jsonld.ts:83` `generateItineraryJsonLd` hardcodes `"Auto-generated ${N}-day itinerary"` — emits on itinerary pages only.
  - `lib/jsonld.ts:263` `generateWebApplicationJsonLd` hardcodes "auto-generate a personalized day-by-day itinerary" — emits on WebApplication schema if rendered.
- **A.12-scoped per-spot provenance**: planner result UI needs per-spot "✓ 책킴 · 정보 확인 YYYY-MM" badge + spot-level `lastVerified` field (full schema in Sprint 3).
- **Sprint 7-scoped micro-disclosure**: per-post "* Klook affiliate link" near CTA buttons. Belongs with affiliate integration.
- **Sprint 7 open decision**: legacy `data/day-courses.ts` 15 placeholder entries → delete / rename (`/day-plans/[id]`) / selectively migrate after 책킴 verification.
- **Sprint 6 remaining `[TODO]` profile URLs**: `FOUNDER.profiles.linkedin/crunchbase/wikidata`. When resolved, Person.sameAs and `/llms-full.txt` auto-expand (no code change needed — both consumers already filter `[TODO]`).
- **A.7 follow-up**: `FOUNDER` doesn't have a canonical `photo` field. If A.11 wires a hero avatar via author-adapter `image?`, consider adding `FOUNDER.photo: "/images/authors/check-kim.jpg"` later for single SoT.
- **Org schema enhancement**: `generateOrganizationJsonLd()` in `lib/jsonld.ts:10` has no `founder` field pointing at FOUNDER. Adding it strengthens entity graph — queue as a low-priority cleanup, not Sprint-A scope.

### Operational notes (carried forward)

- Verification constraint: my IP triggers Vercel Security Checkpoint after 3–5 rapid curls. Stick to 1 curl per deploy + 책킴 View Source for final sign-off.
- Commit identity: inline `GIT_AUTHOR_*` / `GIT_COMMITTER_*` env vars matching `macbook <macbook@macbookui-MacBookPro.local>`. Memory rule: never set global git config.
- Deploy only on "배포해" — always build + local curl verify first.
- Batch pattern: one feature per commit, `.progress.md` log either in the feature commit (if short) or as a following `chore:` commit.

---

### 2026-04-23 — Sprint A.7 session (deployed)

**Completed:**
- **A.7 (expanded)** — `lib/authors.ts` converted to FOUNDER adapter pattern, eliminating the drift risk that surfaced during A.6. Previously `authors.huiwon` duplicated `name` / `nickname` / `role` / `bio` / `expertise` from `/content/brand.ts` and was out of date on 3 of 5 fields (lowercase "tripflowy 창업자", Japan-only bio, 5-entry knowsAbout). Now `authors.huiwon` is just `{ id: "huiwon", brand: FOUNDER }` + optional UI fields. `types/index.ts` `Author` type rewritten to match.
- **New helper `getAuthorIdentity(author, locale)`** in `lib/authors.ts`: single projection of brand identity fields for a given locale, also responsible for filtering `[TODO]` profile URLs into `Person.sameAs`. Shared between JSON-LD emission and author-box UI — eliminates divergent locale-ternary code.
- **`generateArticleJsonLd` author block** rewired through the identity helper. Person schema now emits `name` as the primary public name (책킴 / Check Kim), 7-entry `knowsAbout`, and a 3-URL `sameAs` populated from `FOUNDER.profiles` minus `[TODO]` entries (currently instagram + naverBlog + youtube). Sprint 6 `[TODO]` URLs will auto-join when resolved.
- **Post page UI** (`app/[locale]/posts/[slug]/page.tsx`): byline + author-box switched to identity helper. Redundant `(nickname)` parenthetical dropped — A.8 will reintroduce the bilingual "책킴 (Check Kim)" pair intentionally.
- Commit `0f3181f`.

**Local verification (`/ko/posts/tokyo-disneyland-guide` + `/posts/tokyo-disneyland-guide`):**
- Person.name: `책킴` (ko) / `Check Kim` (en) — primary names, not legal.
- Person.jobTitle: `TripFlowy Founder · 여행 크리에이터` (capital T) / `Founder, TripFlowy · Travel Creator`.
- Person.description: Asia-wide bio exactly matching `FOUNDER.bioKo/bioEn` (markers: "15개국 50개 도시", "50+ cities in 15+ Asian countries", "근교 당일치기 코스", "day-trip routes").
- Person.alternateName: 6-entry array (CheckKim at index 1 from earlier hotfix).
- Person.knowsAbout: 7 entries (`Asia travel`, `Japan travel`, `Tokyo`, `Theme parks`, `Airport transit`, `Observation decks`, `Day trips from major cities`).
- Person.sameAs: `["https://instagram.com/travelkkkim", "https://blog.naver.com/heewon1032", "https://youtube.com/@CheckKim"]`.
- Regression grep: `tripflowy 창업자` 0, `Patrick Kim` 0, `[TODO]` 0, "Auto-generate" etc. all still 0 on homepage.
- `tsc --noEmit` clean, `next build` 258 pages.

**Part F [엔티티 일관성] scorecard after A.7:**
- ✅ Person.alternateName array ≥4 entries (6 actually).
- ✅ Person.jobTitle "TripFlowy Founder" unified across ko + en.
- ✅ Person.sameAs array contains `profiles.instagram` (@travelkkkim) — plus naverBlog + youtube.
- ✅ `Patrick Kim` nowhere in the site.
- ⏭ Author-box UI bilingual name display ("책킴 (Check Kim)") — A.8.

**Partial / deferred (intentional):**
- A.8 "책킴 (Check Kim)" byline format postponed as planned. Paren helper (`${FOUNDER.nameKo} (${FOUNDER.nameEn})`) will live alongside identity helper — ~10 LOC add.
- `lib/authors.ts` `image?`/`url?` fields remain optional on the Author adapter (UI affordance); FOUNDER itself doesn't carry a profile image URL. A later sprint can add `FOUNDER.photo` if/when 책킴 picks a canonical author image.
- Organization/WebSite/WebApplication `alternateName: "트플"` (single string, BRAND-level) remains as-is — not Person-scope.

**Next-session context for A.8:**
- **A.8** (0.5h, 🟡): Byline bilingual display. Target: `app/[locale]/posts/[slug]/page.tsx` author-box headline currently shows just `{identity.name}`. Update to `${FOUNDER.nameKo} (${FOUNDER.nameEn})` on ko pages and `${FOUNDER.nameEn} (${FOUNDER.nameKo})` on en pages. Cleanest implementation: add `displayName` to `getAuthorIdentity` helper — returns the paired form. Consumers stay in one place.
- A.8 also touches the hero byline row at `app/[locale]/posts/[slug]/page.tsx:254, 276` — currently shows `{identity.name}` alone; same pair format? Or keep alone to avoid duplication? Low-stakes design call.

**Open-gap queue (unchanged):**
- `app/[locale]/planner/layout.tsx:18,26,29` — "Auto-Generate Itineraries" / "자동 일정 생성" / "auto-generate verified" → **A.12**.
- `lib/jsonld.ts:83` `generateItineraryJsonLd` hardcodes "Auto-generated ${N}-day" → A.12.
- `lib/jsonld.ts:263` `generateWebApplicationJsonLd` hardcodes "auto-generate a personalized day-by-day itinerary" → A.12.
- Sprint 7 open decision: legacy `data/day-courses.ts` 15 placeholder entries.
- Sprint 6: remaining `[TODO]` URLs in FOUNDER.profiles (linkedin, crunchbase, wikidata).

**Blockers / decisions needed:** None for A.8.

**Files modified this session:**
- `types/index.ts` (Author type rewrite), `lib/authors.ts` (adapter + identity helper), `lib/jsonld.ts` (Article Person block), `app/[locale]/posts/[slug]/page.tsx` (5 consumer sites), `.progress.md`.
- Commit `0f3181f` (5 files, −56/+51 lines — net code shrink from deduplication).

---

### 2026-04-23 — FOUNDER.profiles + alternateName hotfix

**Changes in `/content/brand.ts`:**
- **YouTube URL 정정**: `https://youtube.com/@travelkkkim` → `https://youtube.com/@CheckKim`. Previous value was a cross-wire with the Instagram handle; actual YouTube handle is `@CheckKim` (verified 301 → `https://www.youtube.com/@CheckKim`).
- **Naver blog URL 확정**: `https://blog.naver.com/[TODO]` → `https://blog.naver.com/heewon1032`. First `[TODO]` marker resolved (Sprint 6 originally owned this; resolved early because 책킴 confirmed the handle). Verified 200 OK direct.
- **`FOUNDER.alternateName`** now 6 entries (was 5): added `"CheckKim"` (no-space form) between `"Check Kim"` and `"책킴"`. Rationale: LLMs may not auto-equate the YouTube handle form `CheckKim` with the canonical spaced form `Check Kim` when resolving entity; explicit alias prevents split-entity failures.
- **`heewon1032` intentionally NOT in `alternateName`**: numeric handle identifiers confuse LLM entity disambiguation. It lives only at the `sameAs` URL layer (via `FOUNDER.profiles.naverBlog`) — LLMs that dereference the Naver blog URL can merge via profile metadata rather than treating `heewon1032` as an alias of Check Kim.

**Side effect (positive)**: `/app/llms-full.txt/route.ts` already filters `[TODO]` entries from `FOUNDER.profiles` at render time. After this hotfix deploys, the `### Profiles` block in `/llms-full.txt` gains a third entry (`naverBlog: https://blog.naver.com/heewon1032`) automatically — no route code change needed. LLM training corpus now has a direct path from TripFlowy's full index to Check Kim's Naver body of work.

**Verification:**
- URL reachability (via browser UA):
  - `https://youtube.com/@CheckKim` → 301 → `https://www.youtube.com/@CheckKim` ✅
  - `https://blog.naver.com/heewon1032` → 200 direct ✅
  - `https://instagram.com/travelkkkim` → 301 → `https://www.instagram.com/travelkkkim` ✅
- Local prod `/ko/posts/tokyo-disneyland-guide` Person JSON-LD: `alternateName` = `["Check Kim","CheckKim","책킴","travelkkkim","Huiwon Kim","Chaekkim"]` (6 entries, CheckKim at index 1). `Patrick Kim`: 0. `heewon1032`: 0 (not in alternateName, sameAs not emitted yet — expected until A.7 wiring).
- `tsc --noEmit`: clean. `next build`: 258 pages.

**A.7 implication — sameAs wiring becomes non-optional:**
- `lib/jsonld.ts` `generateArticleJsonLd` currently does `...(author.sameAs?.length ? { sameAs: author.sameAs } : {})` but `authors.huiwon.sameAs` is undefined → no `sameAs` emitted. A.7 must wire Person.sameAs from `FOUNDER.profiles` (filtering `[TODO]`) so the three good URLs (instagram, youtube, naverBlog) reach Person JSON-LD.
- Pattern to use in A.7: `sameAs: Object.values(FOUNDER.profiles).filter((u) => !u.includes("[TODO]"))`. Already established for `/llms-full.txt`; keep it consistent.

**Files modified:**
- `content/brand.ts` (3 targeted edits: 2 URL fixes + 1 alternateName insertion)
- `.progress.md`

---

### 2026-04-23 — Sprint A.6 session (deployed)

**Completed:**
- **A.6** — Person schema `alternateName` array conversion site-wide. Single-file edit in `lib/jsonld.ts`: expanded `@/content/brand` import to include `FOUNDER`, and in `generateArticleJsonLd()` author block swapped `alternateName: author.nickname[locale]` (locale-switched string) to `alternateName: FOUNDER.alternateName` (5-entry array: `Check Kim, 책킴, travelkkkim, Huiwon Kim, Chaekkim`). `generateArticleJsonLd` is the only Person-schema emitter on the site — verified by grep: no inlined `@type: Person` blocks outside `lib/jsonld.ts`, no `/content/guides/` folder, post pages don't override. One helper edit propagates to all 18 post URLs (9 posts × 2 locales). Commit `183055c`.
- **Retroactive documentation** — `/content/AGENTS.md` expanded with three rule sections (user request):
  - Person schema rules (A.6): `alternateName` must always be `FOUNDER.alternateName` array; nickname is UI-only; Patrick Kim permanently retired.
  - Comparison-section copy rules (A.5): column headers use `COMPARISON_HEADINGS` adjectival contrast form, never product-category form. Documents retroactively the `whyDifferent.aiTitle` decision.
  - Metadata-description audit scope (A.5): 4-layer grep (generateMetadata + messages + JSON-LD + static routes). Codifies the A.3/A.4 `<meta>` gap caught in A.5.

**Local verification:**
- `/ko/posts/tokyo-disneyland-guide` Person JSON-LD — `alternateName` = exact 5-entry array, all members present (Check Kim, 책킴, travelkkkim, Huiwon Kim, Chaekkim).
- `/posts/tokyo-disneyland-guide` (EN) — identical array, locale-agnostic emission confirmed.
- Old single-string `alternateName` on post pages: 0 hits.
- `Patrick Kim` grep on both pages: 0 hits.
- `tsc --noEmit` clean, `next build` passes.

**Out-of-scope but verified intact:**
- `Organization.alternateName: "트플"` and `WebSite.alternateName: "트플"` on homepage JSON-LD stay as single strings — these are BRAND-level (not Person); schema.org accepts either form; BRAND identity is one string, not an alias set.
- `WebApplication.alternateName: "트플 여행 플래너"` — same category, kept.
- `lib/authors.ts` `Author.nickname` field preserved (UI byline use; A.8 will replace the display format to `책킴 (Check Kim)` pattern).

**Open-gap queue (unchanged from prior session, for reference):**
- `app/[locale]/planner/layout.tsx:18,26,29` — "Auto-Generate Itineraries" / "자동 일정 생성" / "auto-generate verified" metadata strings. Slated for **A.12** (planner result page overhaul).
- `lib/jsonld.ts:83` `generateItineraryJsonLd` hardcodes `"Auto-generated ${N}-day itinerary"` — also A.12 territory (this schema only emits on itinerary pages).
- `lib/jsonld.ts:263` `generateWebApplicationJsonLd` hardcodes "auto-generate a personalized day-by-day itinerary" — A.12.
- `app/llms.txt/route.ts:26` Cities section: "Auto-generated itinerary from spots personally tested" — lower priority; literal sense, not product positioning. Leave unless it pops up in audit.
- Sprint 7 open decision: legacy `data/day-courses.ts` 15 placeholder entries — delete / rename / migrate.
- `FOUNDER.profiles` `[TODO]` URLs — Sprint 6.

**Next-session context for A.7:**
- **A.7** (0.5h, 🟡): Bio update — expand Japan-only wording to Asia-wide. Touch points:
  - `lib/authors.ts` `huiwon.bio.ko/en` currently says "2007년부터 일본을 20번 넘게…", "Travel creator with 20+ trips to Japan since 2007…". Swap to `FOUNDER.bioKo/bioEn` (which adds "아시아 15개국 50개 도시 이상" / "50+ cities in 15+ Asian countries" + "근교 당일치기 코스" / "day-trip routes").
  - `lib/authors.ts` `huiwon.role.ko` currently "tripflowy 창업자 · 여행 크리에이터" (lowercase + 한국어 '창업자'). Master plan says swap to "TripFlowy Founder · 여행 크리에이터" = `FOUNDER.jobTitleKo`. `role.en` already matches `FOUNDER.jobTitleEn` — verify.
  - `lib/authors.ts` `huiwon.expertise` currently 5 entries; master plan's `FOUNDER.knowsAbout` has 7 ("Asia travel" + "Day trips from major cities" added). Expand.
  - Decision pending: fully derive `huiwon` author from `FOUNDER` (kill `lib/authors.ts` as duplicate SoT), or keep as separate consumer that imports. Lean toward "derive from FOUNDER" to eliminate drift — multiple sessions have now shown `authors.ts` falling behind `brand.ts`. Will flag before executing.

**Blockers / decisions needed for A.7:**
- Whether `lib/authors.ts` should become a thin adapter around `FOUNDER` (recommended — eliminates ongoing bio-drift risk) or stay as independent consumer. Will raise at A.7 start.

**Files created/modified this session:**
- Modified: `lib/jsonld.ts` (1 import line + 1 Person block line), `content/AGENTS.md` (3 new rule sections), `.progress.md`
- Commits: `183055c` (A.6, 3 files)
- No new files, no deletions.

---

### 2026-04-23 — Sprint A.4 + A.5 session (deployed)

**Completed:**
- **A.4** — English WebSite JSON-LD `description` swapped to `META_DESCRIPTIONS.homeEn` in `lib/jsonld.ts` `generateWebSiteJsonLd()` else-branch. Ternary collapsed to one line; both locale branches now import from `@/content/brand`. Local curl `/` → new EN string; prod deploy polling confirmed at least one 200 match on `"Verified travel itinerary templates from a real traveler"` before Vercel security challenge mitigated the IP. Book-ended by 책킴's `/ko` View Source screenshot confirming A.3's ko description live. Commit `86f2941`.
- **A.5** — Hero copy + comparison heading + HowTo JSON-LD + homepage metadata rewrite (expanded scope). Replaced forbidden phrasing across `messages/{ko,en}.json` (hero.heading → `HERO.h1Ko/h1En`, new hero.subTagline → `HERO.subTaglineKo/subTaglineEn`, whyDifferent.heading → `COMPARISON_HEADINGS.ko/en`, howItWorks step titles/descs rewritten with 책킴-authority signal), added `META_DESCRIPTIONS.howToKo/howToEn` to `content/brand.ts` and wired through `generateHowToJsonLd`, rendered `hero.subTagline` slot in `app/[locale]/page.tsx`. Commit `fee2adc`.

**Expanded beyond original A.5 0.5h spec (self-reported):**
- `app/[locale]/page.tsx:27` hardcoded `<meta description>` still carried "자동으로…블로그 검색 그만" (ko) / "auto-generate day-by-day itineraries" (en). A.3/A.4 only touched JSON-LD. Root AGENTS.md §5 is site-wide, so swapped to `META_DESCRIPTIONS.homeKo/homeEn`.
- `whyDifferent.aiTitle` was "AI Travel Planners" / "AI 여행 플래너" — direct match to `BRAND.forbiddenPhrases` category-name form. Rewrote to "AI-Generated Plans" / "AI 생성 일정" (sanctioned adjectival contrast, mirrors `COMPARISON_HEADINGS` pattern). Contrast meaning preserved, banned form gone.

**Local verification (prod build):**
- 11 forbidden-phrase grep checks across ko + en: all **0 hits**. (`AI가 짜주는`, `블로그 그만 보세요`, `블로그 글이 아닌`, `블로그 검색 그만`, `자동으로 맞춤`, `자동으로 하루 코스`, `AI 여행 플래너`, `Stop scrolling blogs`, `auto-generate`, `AI travel planner`, `no generic blog posts`.)
- 11 new-phrase render checks: all present (HERO H1, subTagline, comparison H2, HowTo description, step2 title, meta description — ko + en).
- HowTo JSON-LD `description` on both locales exactly matches `META_DESCRIPTIONS.howToKo/howToEn`.
- `tsc --noEmit` clean, `next build` 258 pages.

**Partial / deferred (intentional):**
- Other pages' noindex meta / metadata still unaudited for forbidden phrases — Sprint A.5 scope was homepage. Planner/posts/tours/hotels layouts have independent `generateMetadata` — grep turned up `app/[locale]/planner/layout.tsx:18,26,29` with "Auto-Generate Itineraries" / "자동 일정 생성" / "auto-generate verified". Queue for A.5 follow-up or absorb into A.9 sitemap/metadata audit.
- `lib/jsonld.ts:83` `generateItineraryJsonLd` + `lib/jsonld.ts:263` `generateWebApplicationJsonLd` still hardcode "Auto-generated" / "auto-generate" — only matters if those schemas get rendered. Homepage doesn't render them; itinerary pages do.
- `/llms.txt` line "Auto-generated itinerary from spots personally tested" is a description of planner output — literal sense of "generated from spots," not product positioning. Lower priority; acceptable.
- `FOUNDER.profiles` `[TODO]` URLs still Sprint 6.

**Next-session context for A.6:**
- **A.6** (0.5h, 🟡): Convert `Person` schema `alternateName` from string → array everywhere. Likely locations (from prior grep):
  - `lib/authors.ts` — author registry; check if authors emit `alternateName` as string
  - `lib/jsonld.ts` `generateArticleJsonLd` (around line 139) — uses `getAuthor()`; may need to spread `FOUNDER.alternateName`
  - Any post/guide page that inlines Person JSON-LD directly
- Verification: `curl` any post page, find `"@type":"Person"` block, confirm `alternateName` is a JSON array with ≥4 elements including "Check Kim", "책킴", "travelkkkim", "Huiwon Kim".
- Also scan for any surviving `"Patrick Kim"` strings (should be 0 per Part G decision — confirm).

**Open gap queue (for later sprints, not blockers):**
- A.5 leftover: `app/[locale]/planner/layout.tsx` metadata still "auto-generate" (title + og + description). Recommend absorbing into A.9 or A.5b micro-fix.
- A.2 follow-up: Sprint 7 entry will need to decide fate of legacy `data/day-courses.ts` 15 placeholder entries (delete / rename / selective migrate).
- `lib/jsonld.ts` itinerary + webapp schemas: forbidden-phrase cleanup when we touch those rendering paths.

**Blockers / decisions needed:**
- None outstanding for A.6.
- Ongoing verification constraint: my IP keeps triggering Vercel Security Checkpoint after ~3–5 rapid curls. Mitigation established — rely on 책킴's View Source for prod confirmation; single 90–120s post-deploy curl attempt from my side; Vercel dashboard as alternate signal.

**Files created/modified this session:**
- Modified: `lib/jsonld.ts` (A.4 EN swap + A.5 HowTo desc swap), `content/brand.ts` (A.5 + `META_DESCRIPTIONS.howToKo/howToEn`), `messages/ko.json` + `messages/en.json` (A.5 hero/comparison/howItWorks), `app/[locale]/page.tsx` (A.5 subTagline slot + metadata SoT), `.progress.md`
- Commits: `86f2941` (A.4, 2 files), `fee2adc` (A.5, 6 files)
- No new files, no deletions.

---

### 2026-04-23 — Sprint A.2 + A.3 session (deployed)

**Completed:**
- **A.2** — `/courses` unblock via hybrid option C. Removed `Disallow: /courses*` from all 10 UA blocks in `public/robots.txt`. Kept legacy `/courses/[id]` `noindex/nofollow` meta and sitemap exclusion (placeholder-quality `data/day-courses.ts` catalog, pre-existing developer decision honored). Header/Footer nav confirmed 0 `/courses` refs — no component edits needed. Added new `/courses/[city]/[slug]` route rules to `app/AGENTS.md` + `.progress.md` Sprint 7 (index:true, self-canonical, sitemap inclusion, hreflang, dual schema, visitedAt/lastVerified, 책킴-verified only). Commit `45ef4ac`.
- **A.3** — Korean WebSite JSON-LD description swapped. `lib/jsonld.ts` `generateWebSiteJsonLd()` ko branch now imports `META_DESCRIPTIONS.homeKo` from `@/content/brand`; AI-positioning string ("자동으로 여행 일정을 만들어주는 여행 플래너") removed entirely. Commit `6408569`.

**Production verification:**
- `robots.txt` `/courses` refs: 0 (down from 40 lines across 10 UA blocks).
- `/courses/danang-my-khe-beach` → 200 OK, `<meta name="robots" content="noindex, nofollow">` intact, canonical self-referential.
- `sitemap.xml` `/courses/*`: 0 entries (14 total URLs, unchanged from baseline).
- `/ko` WebSite JSON-LD `description` = exact `META_DESCRIPTIONS.homeKo` string; legacy ko string: 0 grep hits; new ko string: 2 hits (JSON-LD + meta description).
- `tsc --noEmit` clean, `next build` 258 pages.

**Partial / deferred (intentional):**
- **A.4** queued next — `lib/jsonld.ts:46` English branch still hardcoded `"Auto-generate day-by-day travel itineraries…"`. Target swap: `META_DESCRIPTIONS.homeEn`. Same file, ternary else-branch; trivial one-line change.
- Legacy `/courses/[id]` catalog disposition (delete / rename to `/day-plans/[id]` / selective 책킴-migration) deferred to Sprint 7 entry.
- `FOUNDER.profiles` `[TODO]` URLs still placeholder (Sprint 6).

**Next-session context for A.4 → A.5 → …:**
- **A.4** (0.2h, 🟠): edit `lib/jsonld.ts` else-branch to `META_DESCRIPTIONS.homeEn`. Verify on prod with `curl https://www.tripflowy.com/ | grep "WebSite"`.
- **A.5** (0.5h, 🟠): hero H1/subtitle copy across `app/[locale]/page.tsx` (+ any HeroSection component under `components/`) → `HERO.h1Ko/h1En`, `HERO.subTaglineKo/subTaglineEn`. Also update comparison section H2 → `COMPARISON_HEADINGS.ko`. Grep for "블로그, 유튜브, 가이드북 그만 보세요" / "Stop scrolling blogs" first — expected to exist in the hero component. Removal is mandatory (forbiddenPhrases enforcement).
- **A.6** (0.5h, 🟡): search for any `alternateName:` usage emitting a single string; replace with `FOUNDER.alternateName` array. Likely in `lib/authors.ts` + any Article author JSON-LD helper in `lib/jsonld.ts` (`generateArticleJsonLd` around line 139).
- Batch reminder: user's `/content/brand.ts` constants now proven stable through 3 consumer paths (routes, JSON-LD, and about to include hero). Keep importing; do not hardcode.

**Blockers / decisions needed:**
- None outstanding. A.2 required scope clarification (legacy catalog is placeholder) — decided option C. Future Sprint 7 entry will revisit legacy catalog disposition.
- Deploy flow continues to work via inline `GIT_AUTHOR_*` env vars matching existing repo identity `macbook <macbook@macbookui-MacBookPro.local>`. No global git config set (per memory rule).

**Files created/modified this session:**
- Modified: `public/robots.txt` (−40 lines), `app/AGENTS.md` (routing rules expanded), `lib/jsonld.ts` (+1 import, ko description → brand import), `.progress.md` (A.2, A.3 logs + Sprint 7 rule additions)
- Commits: `45ef4ac` (A.2, 3 files), `6408569` (A.3, 2 files)
- Unchanged (verified intact): `app/[locale]/courses/*` (noindex meta + canonical), `app/sitemap.ts` (courses exclusion), `components/layout/{Header,Footer}.tsx` (no courses links)

---

### 2026-04-23 — Sprint A.1 session (deployed)

**Completed:**
- **A.1** — `/llms.txt` 500 error fixed + `/llms-full.txt` new. Root cause was route non-existence → `[locale]` dynamic segment matched `locale="llms.txt"` → next-intl runtime error → 500. Fix: static Route Handlers (`force-static`, `text/plain`) at `app/llms.txt/route.ts` and `app/llms-full.txt/route.ts`. Both import from `@/content/brand` (no hardcoded strings); `llms-full.txt` filters `[TODO]` entries from `FOUNDER.profiles` before emitting.
- `public/robots.txt` — added `# LLM-friendly index` comment block above `Sitemap:` lines.
- Committed as two separate commits (Sprint 0 infra + A.1 feature) and pushed to `origin/main`.

**Production verification (https://www.tripflowy.com):**
- `/llms.txt` → 200 OK, 3.9KB, `TripFlowy`/`Check Kim`/`책킴` all present (1·1·1).
- `/llms-full.txt` → 200 OK, 169KB, brand markers (21·5·5), all 9 Tokyo posts (title/excerpt/body/FAQ/CTA) bilingual.
- `/robots.txt` → 200 OK, LLM-friendly index block visible.
- `[TODO]` leakage: **0** in both routes.
- Forbidden phrases (`AI travel planner`, `GPT-based`, `Stop scrolling blogs`, `AI가 짜주는`, `블로그 그만 보세요`): **0** in both routes.
- apex→www redirect resolves via 307 to `www.tripflowy.com`, final status 200 on both bots (ClaudeBot, GPTBot).

**Partial / deferred (intentional):**
- `llms-full.txt` currently lists 9 Tokyo posts only (the full current catalog). Will auto-expand as `data/posts.ts` grows — no code change needed when Sprint 7 adds Osaka/Kyoto/etc.
- Day Trips section in `llms.txt` is narrative-only (Kawagoe/Chichibu/Mt. Fuji/Nara/Himeji listed without links) since `/courses/*` pages for day trips don't exist yet. Will re-emit as linked entries once Sprint 7 ships them.
- `FOUNDER.profiles` still has `[TODO]` for naverBlog/linkedin/crunchbase/wikidata — Sprint 6.

**Next-session context (Sprint A.2 start):**
- `public/robots.txt` currently has `Disallow: /courses*` entries for **10 user-agents** (`*`, `Googlebot`, `Yeti`, `GPTBot`, `ChatGPT-User`, `OAI-SearchBot`, `PerplexityBot`, `Perplexity-User`, `ClaudeBot`, `Claude-User`, `Google-Extended`). All need removing.
- Nav removal: grep `components/` and `app/` for `/courses` href literals. Header/footer components likely candidates.
- Do **not** add `noindex` meta on `/courses/[id]` — these should stay indexable for LLM/Google while being hidden from user nav.
- Confirm `sitemap.ts` emits `/courses/*` paths (check `app/sitemap.ts`) since A.9 audits that, but quick check now de-risks A.2.

**Blockers / decisions needed:** None. `git config` was absent globally — sidestepped by inline `GIT_AUTHOR_*` / `GIT_COMMITTER_*` env vars matching the last-commit identity (`macbook <macbook@macbookui-MacBookPro.local>`). Memory rule "NEVER update the git config" respected. User may want to set `git config --global user.email` separately at some point, but not a session blocker.

**Files created/modified this session:**
- Created: `app/llms.txt/route.ts`, `app/llms-full.txt/route.ts`
- Modified: `public/robots.txt`, `.progress.md`
- Commits: `49bf220` (sprint-0 infra, 9 files), `e4decab` (A.1 routes, 3 files)

---

### 2026-04-23 — Sprint 0 session

**Completed (all 8 sub-tasks):**
1. Root `AGENTS.md` rewritten (5.7KB, <200 lines) — project overview, tech stack, brand single-source-of-truth, entity consistency, positioning rules, MVP scope, workflows, context economy.
2. `CLAUDE.md` → `AGENTS.md` symlink (Cursor/Aider/Codex cross-compat).
3. `/app/AGENTS.md` — App Router + JSON-LD dual-type + hreflang bidirectional + `Person.alternateName` array rule + `.tf-serif` English-only gating + `[TODO]` filter rule for `sameAs`.
4. `/content/AGENTS.md` — frontmatter standard, 40–80 word extraction chunk rule, `TRUST_PHRASES` requirement, `forbiddenPhrases` grep commands, EN adaptation rules, Klook URL convention (no `/ko/`).
5. `/docs/AGENTS.md` — internal Markdown vs public `/app/[locale]/docs/` split; `.progress.md` as the progress source (don't edit the master plan to log progress).
6. `/scripts/AGENTS.md` — `sharp.withMetadata()` EXIF preservation, Naver→TripFlowy conversion rules (트립플로위→트플, KRW strip in EN, idiom replacement), `process.env.ANTHROPIC_API_KEY`, `npx tsx` execution.
7. `/content/brand.ts` generated from master-plan Part B with `as const` on every export — `BRAND`, `FOUNDER`, `META_DESCRIPTIONS`, `HERO`, `COMPARISON_HEADINGS`, `TRUST_PHRASES`, `AFFILIATE_DISCLOSURE`.
8. `tsc --noEmit` → exit 0, no errors, no regressions.

**Partial / deferred (intentional):**
- `FOUNDER.profiles.naverBlog` / `.linkedin` / `.crunchbase` / `.wikidata` still contain `[TODO]` placeholders. These are deliberately held for **Sprint 6** when Wikidata/Crunchbase items are created and real URLs are known. The `/app/AGENTS.md` `sameAs` filter rule is in place so these never leak into production JSON-LD in the meantime.
- Sprint 1/3/4/5/6/7 tracked at top-level checkbox granularity only — sub-tasks get exploded at sprint-entry time (saves maintenance churn on plan drift).

**Next-session context for Sprint A:**
- Start with **A.1** (`/llms.txt` currently returns 500 — highest urgency, blocks ClaudeBot / PerplexityBot entity learning).
- Then A.2 (`robots.txt` unblock `/courses` + remove from nav), then A.3/A.4 (WebSite schema description swap — now trivial since `META_DESCRIPTIONS` is importable).
- **All 12 A-fixes can now `import` from `@/content/brand`** — no more hardcoded strings. Specifically: A.3/A.4 use `META_DESCRIPTIONS.home{Ko,En}`; A.5 uses `HERO.*` + `COMPARISON_HEADINGS.ko`; A.6 uses `FOUNDER.alternateName` (array); A.7 uses `FOUNDER.bio{Ko,En}` + `FOUNDER.jobTitle{Ko,En}`; A.8 uses `${FOUNDER.nameKo} (${FOUNDER.nameEn})`; A.10 uses `AFFILIATE_DISCLOSURE.en`; A.11 uses `FOUNDER.stats`.
- Before A.1 coding, locate the current `/llms.txt` route — check `/app/llms.txt/route.ts` (likely location per Next.js 16 App Router). If different, grep `llms` under `/app`.

**Blockers / decisions needed:** None. All Part G pre-decisions were already settled in the master plan.

**Files created/modified this session:**
- Created: `/app/AGENTS.md`, `/content/AGENTS.md`, `/content/brand.ts`, `/docs/AGENTS.md`, `/scripts/AGENTS.md`, `/.progress.md`
- Rewritten: `/AGENTS.md`
- Replaced: `/CLAUDE.md` (file → symlink to `AGENTS.md`)
- New directory: `/content/` (did not exist before this session)
